# 服务器档：PaddleOCR-VL + vLLM
# 用法：PROFILE=server bash scripts/manage.sh deploy
ENGINE=vl
GPU_ID=2
PORT=8710
WORKERS=4
MODEL_SOURCE=modelscope
# 该机 RTX 5090 是 sm_120，Paddle 官方 wheel 不支持，直接定 cpu 省掉 2 分钟探测
DEVICE=cpu
